[build]
builder = "dockerfile"

[deploy]
startCommand = "java -jar app.jar"
healthcheckPath = "/highlowjack/setup"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
